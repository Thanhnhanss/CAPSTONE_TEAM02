@model VanLangDoctor.Models.DAT_LICH

@{
    ViewBag.Title = "Đăng ký lịch trực";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}


<div class="page-wrapper" style="display: block;">
    <div class="page-breadcrumb">
        <div class="row">
            <div class="col-7 align-self-center">
                <h4 class="page-title text-truncate text-dark font-weight-medium mb-1">@ViewBag.Title</h4>
                <div class="d-flex align-items-center">
                    <nav aria-label="breadcrumb">
                        <ol class="breadcrumb m-0 p-0">
                            <li class="breadcrumb-item"><a href="~/trang-chu-quan-ly" class="text-muted">Trang chủ</a></li>
                            <li class="breadcrumb-item"><a href="~/trang-chu-quan-ly/bac-si/danh-sach" class="text-muted">Bác sĩ</a></li>
                            <li class="breadcrumb-item text-muted active" aria-current="page">@ViewBag.Title</li>
                        </ol>
                    </nav>
                </div>
            </div>
            <div class="col-5 align-self-center">
                <div class="customize-input float-right">
                    <a class="button-back" href="@Url.Action("Index", "QL_DatLich")">
                        <span-back>Trở về</span-back>
                    </a>
                </div>
            </div>
        </div>
    </div>
    <div class="container-fluid">
        <div class="card">
            <div class="card-body">
                @if (TempData["Success"] != null)
                {
                    <div class="alert alert-warning alert-dismissible fade show" role="alert">
                        <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                            <span aria-hidden="true" style=" float: right; margin-top: -10px">×</span>
                        </button>
                        <span><strong>@TempData["Success"]</strong></span>
                    </div>
                }

                @using (Html.BeginForm())
                {
                    @Html.AntiForgeryToken()

                    <div class="form-horizontal">
                        <h4>Đăng ký lịch trực</h4>
                        <hr />
                        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                        <div class="form-group">
                            <label for="ngay_truc">Ngày trực</label>
                            <input type="datetime-local" class="form-control" name="NGAY_TRUC" min="@DateTime.Now.AddDays(1).ToString("yyyy-MM-ddT08:00")" style="border-radius: 15px; box-shadow: 4px 4px 10px rgb(0 0 0 / 20%) " required />
                        </div>

                        <div class="form-group">
                            <div class="col-md-10">
                                <button type="submit" class="button" style="vertical-align:middle"><span>Đăng ký </span></button>
                            </div>
                        </div>
                    </div>
                }
            </div>
        </div>
    </div>
</div>